# Language Maintenance

- `en_US` docs are canonical for current behavior and release details.
- `zh-CN` docs are maintained as a high-value mirror and may lag on non-breaking refinements.
- For breaking changes, migration pages must be synced in both `en_US` and `zh-CN` during the same release cycle.

# Uninstall

Remove symlink and all managed assets under `~/.envlock`:

```bash
curl -fsSL https://raw.githubusercontent.com/PerishCode/envlock/main/scripts/uninstall.sh | sh
```

This workflow is intentionally directory-scoped so uninstall is safe and predictable.

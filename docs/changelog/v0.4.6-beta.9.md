# v0.4.6-beta.9

- Generate manager wrapper scripts for `npm`, `pnpm`, and `yarn` so manager-specific environment stays isolated instead of leaking into the whole shell.
- Remove the npm 11 `Unknown env config "store-dir"` warning while preserving pnpm store routing.
- Keep selected-node routing stable for manager shims and restore `yarn global add` to plugin-controlled install paths.

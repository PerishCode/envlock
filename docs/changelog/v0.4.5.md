# v0.4.5

- Fix `install.sh` so the documented `sh`-based install path works in minimal shells and containers.
- Fix `envlock plugin node` override error handling so explicit binary failures do not fall through to misleading PATH fallback messages.
- Add a minimal release gate that requires `docs/changelog/<tag>.md` to exist before publishing stable or beta releases.

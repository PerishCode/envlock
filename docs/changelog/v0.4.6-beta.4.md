# v0.4.6-beta.4

- Fix npm global prefix layout creation for multi-version plugin node flows by creating `global/bin` and `global/lib/node_modules`.
- Fix pnpm store routing by emitting `npm_config_store_dir`, so real pnpm installs use the plugin-selected versioned store.
- Validate Node 18/22/24 coexistence with mixed npm, pnpm, and yarn versions under install-based Docker E2E.

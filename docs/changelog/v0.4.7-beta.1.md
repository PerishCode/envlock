# v0.4.7-beta.1

- Keep Yarn classic global package contents under envlock-managed directories while ensuring global executables become discoverable through envlock-managed PATH entries.
- Preserve the package-manager-aware pnpm and yarn routing fixes from `v0.4.6-beta.10`.
- Continue local fully isolated dogfooding of an envlock-owned Node ecosystem under `~/.envlock` without depending on host-installed Node managers.

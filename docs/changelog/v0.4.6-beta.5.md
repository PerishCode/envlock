# v0.4.6-beta.5

- Fix manager shim version resolution when users explicitly select a Node binary but `npm`/`pnpm`/`yarn` launchers rely on `#!/usr/bin/env node`.
- Add regression coverage for explicit `--node-bin` plus manager shims under a minimal PATH.
- Continue brutal multi-version validation for Node 18/22/24 coexistence with mixed manager versions.

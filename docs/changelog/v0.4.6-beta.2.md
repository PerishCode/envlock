# v0.4.6-beta.2

- Fix `plugin node` failure-path logging so stderr always points to the per-invocation log file.
- Ensure plugin subcommands go through the same top-level failure/logging finalization path as other CLI commands.
- Add regression coverage for the missing `See log:` failure case.

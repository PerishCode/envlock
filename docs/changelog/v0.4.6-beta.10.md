# v0.4.6-beta.10

- Make manager version probing run outside project `packageManager` interference so `plugin node apply` works inside pnpm-managed repositories.
- Make `pnpm` and `yarn` wrappers resolve their runtime version in the caller's working directory, so cache/store/global routing follows project-selected corepack versions.
- Add regression coverage for package-manager projects where apply-time and runtime manager versions differ.
